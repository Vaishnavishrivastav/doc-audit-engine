import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const metadata = {
  title: 'SmartAudit AI — AI-Powered Invoice Auditor',
  description: 'Automate invoice audit workflows, detect billing errors, duplicate line items, tax discrepancies, and compliance violations instantly with AI.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
